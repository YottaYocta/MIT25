'use client';

import { ResponsiveControlsProps } from '../types';

export function ResponsiveControls({
  currentIndex,
  totalViews,
  onNavigate,
  currentTrinket,
  showMetadata = true
}: ResponsiveControlsProps) {
  return null;
}

